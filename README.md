# image-policy-operator

Kubernetes operator / validating webhook for image policy enforcement.

## Docs

- `docs/environment-variables.md` — cấu hình Telegram bằng biến môi trường + Kubernetes Secret (`telegram-credentials`).